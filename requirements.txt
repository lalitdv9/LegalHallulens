# LegalHalluLens — Python dependencies
# Tested with Python 3.12.

# Data analysis
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Notebook / display
jupyter>=1.0.0
ipykernel>=6.25.0
ipython>=8.0.0

# LLM clients
openai>=1.30.0
anthropic>=0.30.0
google-genai>=1.0.0
ollama>=0.4.0
tiktoken>=0.7.0

# Multi-agent debate pipeline
langgraph>=0.2.0
langchain-core>=0.3.0

# HTTP / TLS
httpx>=0.28.0
certifi>=2023.7.22
python-dotenv>=1.0.0

# Optional: only required if tokenizing Llama models exactly via HF tokenizer
# (utils/llm.py imports lazily; remove if you don't need exact Llama token counts)
transformers>=4.40.0
